# B1 shared auth/session core

## Summary

- Moved the shared session readers and teacher-directory session writer from
  `app.py` to `backend/core/auth.py`.
- Kept the five helper names re-exported by `app.py`; routers and deployment
  imports therefore retain their existing compatibility surface.
- No routes, cookies, database tables, or production behavior were changed.

## Verification

Commands run:

```bash
APP_ENV=test SECRET_KEY=test-secret PYTHONPATH=tests/stubs \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /opt/miniconda3/bin/pytest -q \
  tests/test_auth_core.py tests/test_app_factory.py \
  tests/test_backend_baseline_contracts.py \
  tests/test_database_session_boundary.py tests/test_model_schema_boundaries.py
PATH=/opt/miniconda3/bin:/usr/local/bin:$PATH make test-static
```

Results:

- Focused compatibility and package-boundary tests: 8 passed.
- Backend static suite: 172 passed.
- Full parallel gate: previously passed in `.tmp/test-logs/20260711-003807/`
  before this isolated helper extraction; the focused and static checks cover
  the changed boundary.

## Known issues

- `app.py` remains a compatibility composition entry point and still contains
  domain and integration implementations targeted by later B1/B2 slices.
- Legacy browser routes remain intentionally retained.

## Next best step

- Extract the teacher-directory query workflow into a repository/service pair,
  preserving its current response DTO and route contracts.
